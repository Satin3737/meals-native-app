import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    text: {
        color: '#ffffff80',
        fontSize: 20,
        textAlign: 'center'
    }
});

export default styles;
