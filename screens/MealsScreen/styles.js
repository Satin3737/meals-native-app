import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    screen: {
        flex: 1,
        paddingVertical: 16
    },
    gap: {
        height: 16
    }
});

export default styles;
